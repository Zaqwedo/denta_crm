# Denta CRM

Система управления стоматологической клиникой с авторизацией через Google, Yandex и обычным логином.

## 🚀 Быстрый старт

### 1. Клонирование и установка
```bash
git clone <repository-url>
cd denta-crm
npm install
```

### 2. Настройка переменных окружения
Создайте файл `.env.local` в корне проекта:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Yandex OAuth
YANDEX_CLIENT_ID=your-yandex-client-id
YANDEX_CLIENT_SECRET=your-yandex-client-secret
YANDEX_REDIRECT_URI=https://your-domain.vercel.app/api/auth/yandex/callback

# Application
APP_URL=http://localhost:3000
```

### 3. Настройка Supabase

1. Создайте новый проект в [Supabase](https://supabase.com)
2. Выполните SQL скрипт `supabase-setup-rls.sql` в SQL Editor
3. Настройте Row Level Security (RLS) политики

### 4. Настройка безопасности и доступа

Для ограничения доступа настройте белые списки email (подробно описано в разделе "Безопасность и управление доступом"):

1. Откройте `app/contexts/AuthContext.tsx`
2. Добавьте разрешенные email в `ALLOWED_EMAILS`
3. Для Yandex OAuth настройте `ALLOWED_YANDEX_EMAILS` в `app/patients/GoogleAuthHandler.tsx`

### 5. Настройка Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект или выберите существующий
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **+ CREATE CREDENTIALS** → **OAuth 2.0 Client IDs**
5. Если OAuth consent screen не настроен:
   - Нажмите **Configure consent screen**
   - Выберите **External** (для публичного доступа)
   - Заполните информацию о приложении
   - Добавьте тестовых пользователей (ваш email)
   - Сохраните и вернитесь к созданию credentials
6. Выберите тип приложения: **Web application**
7. Добавьте **Authorized redirect URIs**:
   - `http://localhost:3000/api/auth/google/callback` (для разработки)
   - `https://ваш-домен.vercel.app/api/auth/google/callback` (для продакшена)
8. Скопируйте `Client ID` и `Client Secret`

### 6. Настройка Yandex OAuth

1. Перейдите в [Yandex OAuth](https://oauth.yandex.com)
2. Создайте новое приложение
3. Выберите тип: "Веб-сервисы"
4. Укажите название приложения
5. Добавьте **Callback URI** (важно: должен точно совпадать):
   - **Локальная разработка**: `http://localhost:3000/api/auth/yandex/callback`
   - **Продакшн**: `https://ваш-домен.vercel.app/api/auth/yandex/callback`
6. Скопируйте `Client ID` и `Client Secret`
7. Добавьте переменную `YANDEX_REDIRECT_URI` в Vercel (продакшн URL)
8. **Рекомендация**: Настройте белый список email для безопасности (см. раздел "Безопасность и управление доступом")

### 7. Запуск приложения
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🔐 Безопасность и управление доступом

### Белые списки Email

Приложение поддерживает ограничение доступа по email адресам для повышения безопасности. Это позволяет контролировать, какие пользователи могут авторизоваться.

#### Настройка белого списка для обычной авторизации

1. Откройте файл `app/contexts/AuthContext.tsx`
2. Найдите константу `ALLOWED_EMAILS`
3. Добавьте разрешенные email адреса:

```typescript
const ALLOWED_EMAILS: string[] = [
  'admin@denta-crm.com',
  'doctor@clinic.com',
  'nurse@clinic.com'
]
```

**Если список пустой** - доступ разрешен для всех email.

#### Настройка белого списка для Yandex OAuth

1. Откройте файл `app/patients/GoogleAuthHandler.tsx`
2. Найдите константу `ALLOWED_YANDEX_EMAILS`
3. Добавьте разрешенные Yandex email адреса:

```typescript
const ALLOWED_YANDEX_EMAILS: string[] = [
  'user@yandex.ru',
  'admin@yandex.com',
  'doctor@yandex.ru'
]

const ALLOWED_GOOGLE_EMAILS: string[] = [
  'user@gmail.com',
  'admin@gmail.com',
  'doctor@gmail.com'
]
```

#### Правила работы:

- **Регистрозависимость**: `user@yandex.ru` ≠ `User@yandex.ru`
- **Точная проверка**: Email должен полностью совпадать
- **Отдельные списки**: Для каждого типа авторизации свой список
- **Динамическое обновление**: Изменения применяются сразу после перезапуска

#### Примеры использования:

```typescript
// Разрешить только администраторов
const ALLOWED_EMAILS = ['admin@clinic.com']

// Разрешить всю команду
const ALLOWED_EMAILS = [
  'admin@clinic.com',
  'doctor1@clinic.com',
  'doctor2@clinic.com',
  'nurse1@clinic.com'
]

const ALLOWED_YANDEX_EMAILS = [
  'admin@yandex.ru',
  'doctor1@yandex.ru'
]

const ALLOWED_GOOGLE_EMAILS = [
  'admin@gmail.com',
  'doctor1@gmail.com'
]

// Отключить ограничения (доступ всем)
const ALLOWED_EMAILS = []
const ALLOWED_YANDEX_EMAILS = []
const ALLOWED_GOOGLE_EMAILS = []
```

#### Безопасность:

- **Рекомендация**: Всегда используйте белые списки в продакшене
- **Мониторинг**: Проверяйте логи на попытки несанкционированного доступа
- **Регулярное обновление**: Периодически пересматривайте списки пользователей

## 🔧 Структура проекта

```
denta-crm/
├── app/                    # Next.js 13+ App Router
│   ├── api/               # API маршруты
│   ├── contexts/          # React Context (AuthContext)
│   ├── patients/          # Страницы пациентов
│   │   ├── [id]/         # Детальная страница пациента
│   │   ├── actions.ts    # Server Actions
│   │   └── page.tsx      # Главная страница списка
│   └── layout.tsx        # Корневой layout
├── lib/                   # Утилиты и конфигурация
│   ├── constants.ts      # Константы (врачи, медсестры, статусы)
│   ├── supabase.ts       # Клиент Supabase
│   └── supabase-db.ts    # Функции работы с БД
└── pages/api/            # API маршруты (Pages Router)
    └── auth/             # Авторизация
```

## 🚀 Деплой

### Vercel

1. Подключите репозиторий к [Vercel](https://vercel.com)
2. Добавьте переменные окружения в Vercel Dashboard
3. Автоматический деплой при push в main ветку

### Ручной деплой
```bash
npm run build
npm start
```

## 🎯 Быстрые команды

```bash
# Локальная разработка
npm run dev

# Деплой на Vercel
git push origin main

# Проверка безопасности
node test-rls-security.js

# Настройка БД
# Выполнить supabase-setup-rls.sql в Supabase SQL Editor
```

**Приятной разработки! 👨‍⚕️🦷**

---

## 📝 Лицензия

Этот проект предназначен только для личного использования.