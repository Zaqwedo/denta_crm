# 🔒 Исправление утечки Telegram Bot Token

## ⚠️ КРИТИЧНО: Токен был скомпрометирован

GitHub Security обнаружил утечку Telegram Bot Token в файле `start-bot.sh`.

## 🚨 Немедленные действия:

### 1. Отзовите скомпрометированный токен

**ВАЖНО:** Токен `8524858941:AAG_-6goRMQJELZUKZmtLRa0uhegIdcKzcs` был публично раскрыт и должен быть немедленно отозван.

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/revoke` или `/mybots`
3. Выберите вашего бота
4. Выберите "API Token" → "Revoke current token"
5. Создайте новый токен

### 2. Обновите все места, где используется токен

После создания нового токена:
- Обновите переменные окружения (`.env`, `.env.local`)
- Обновите конфигурационные файлы
- Обновите все сервисы, использующие этот токен

### 3. Удалите файл из истории Git (если он там есть)

Если файл `start-bot.sh` был закоммичен в Git:

```bash
# Удалить файл из истории Git
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch start-bot.sh" \
  --prune-empty --tag-name-filter cat -- --all

# Принудительно обновить удаленный репозиторий
git push origin --force --all
git push origin --force --tags
```

**ВНИМАНИЕ:** `git filter-branch` изменяет историю Git. Убедитесь, что все участники проекта синхронизированы.

### 4. Альтернативный способ (BFG Repo-Cleaner)

```bash
# Установить BFG (если еще не установлен)
# brew install bfg  # для macOS

# Удалить файл из истории
bfg --delete-files start-bot.sh

# Очистить и обновить
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# Принудительно обновить удаленный репозиторий
git push origin --force --all
```

## ✅ Профилактика на будущее:

1. **Никогда не коммитьте секреты в Git:**
   - Используйте переменные окружения
   - Добавляйте файлы с секретами в `.gitignore`
   - Используйте `.env.local` для локальных секретов

2. **Проверьте `.gitignore`:**
   - Убедитесь, что `*.sh` файлы добавлены (уже добавлено)
   - Убедитесь, что `.env*` файлы игнорируются (уже добавлено)

3. **Используйте GitHub Secrets:**
   - Для CI/CD используйте GitHub Secrets
   - Для Vercel используйте Environment Variables в панели управления

4. **Регулярно проверяйте:**
   - Используйте [git-secrets](https://github.com/awslabs/git-secrets)
   - Используйте [truffleHog](https://github.com/trufflesecurity/trufflehog)
   - Проверяйте Security tab в GitHub

## 📝 Шаблон для start-bot.sh (если нужен):

```bash
#!/bin/bash

# Используйте переменную окружения вместо хардкода токена
TELEGRAM_BOT_TOKEN="${TELEGRAM_BOT_TOKEN}"

if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
    echo "Ошибка: TELEGRAM_BOT_TOKEN не установлен"
    echo "Добавьте в .env.local:"
    echo "TELEGRAM_BOT_TOKEN=your_token_here"
    exit 1
fi

# Ваш код здесь
```

## 🔗 Полезные ссылки:

- [GitHub: Removing sensitive data from a repository](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [@BotFather](https://t.me/BotFather)
