# 🚨 Исправление ошибки EmailJS: "The recipients address is empty"

## ❗ Проблема
EmailJS возвращает ошибку "The recipients address is empty", что означает неправильную настройку получателя в шаблоне.

## 🔧 Решение

### Вариант 1: Исправить шаблон EmailJS (рекомендуется)

1. **Зайдите на https://www.emailjs.com/**
2. **Перейдите в "Email Templates"**
3. **Найдите ваш шаблон** (ID: template_yvrojpm)
4. **В разделе "Settings" исправьте:**

   **To Email (получатель):**
   ```
   {{to_email}}
   ```

   **Variables (переменные):**
   - `verification_code` - Required: Yes
   - `to_email` - Required: Yes

5. **Сохраните и протестируйте**

### Вариант 2: Настроить фиксированный получатель

Если вы хотите отправлять все коды на один email:

1. **В настройках шаблона укажите фиксированный email:**
   ```
   To Email: your-email@example.com
   ```

2. **Уберите to_email из переменных шаблона**

3. **В коде измените параметры:**
   ```javascript
   // В lib/email.ts измените параметры на:
   {
     verification_code: options.text.match(/(\d{6})/)?.[1] || '000000'
     // убрать to_email
   }
   ```

### Вариант 3: Использовать Gmail SMTP (надежнее)

Если EmailJS не работает, настройте Gmail:

1. **Следуйте инструкции:** `GMAIL_SETUP.md`
2. **Создайте App Password в Gmail**
3. **Добавьте в `.env.local`:**
```env
GMAIL_USER=your-gmail@gmail.com
GMAIL_APP_PASSWORD=your-16-digit-password
```

## 🧪 Тестирование

После исправления:
1. **Перезагрузите приложение:** `npm run dev`
2. **Протестируйте вход по email**
3. **Проверьте логи браузера** (F12 > Console)

## 📞 Поддержка

Если проблема остается:
- Проверьте лимиты EmailJS (200 email/месяц бесплатно)
- Попробуйте Gmail SMTP - он надежнее
- Проверьте настройки email сервиса в EmailJS