# Настройка автоматического деплоя Vercel

## 🚀 Как работает автоматический деплой

### 1. GitHub + Vercel интеграция
- **Автоматически**: При push в `main` ветку Vercel запускает деплой
- **Время**: 1-3 минуты после push
- **Условия**: Изменения в коде, не в документации

### 2. Проверка настроек интеграции
```
Vercel Dashboard → Settings → Integrations → GitHub
```
- Должен быть активный GitHub коннектор
- Права: `repo`, `workflow`, `user:email`

---

## 🔧 Настройка триггеров деплоя

### Автоматические триггеры:

#### 1. **Push в main ветку** (по умолчанию)
```bash
git push origin main  # Автоматически запускает деплой
```

#### 2. **Pull Request** (опционально)
- Создайте PR → Vercel создаст preview деплой
- Полезно для тестирования изменений

#### 3. **Ветки для деплоя**
```
Vercel Dashboard → Settings → Git
- Production Branch: main
- Preview Branches: Все остальные
```

---

## 🎯 Ручное управление деплоем

### 1. **Trigger Deploy вручную**
```
Vercel Dashboard → Your Project → Deployments → Trigger Deploy
```
- Выберите ветку (обычно main)
- Нажмите "Trigger"

### 2. **Redeploy существующего**
```
Vercel Dashboard → Deployments → ⋯ → Redeploy
```

### 3. **Отмена деплоя**
```
Vercel Dashboard → Deployments → ⋯ → Cancel
```

---

## 📊 Мониторинг деплоев

### Статусы деплоя:
- **🟡 Building** - В процессе сборки
- **🟢 Ready** - Успешно развернуто
- **🔴 Error** - Ошибка сборки
- **⚪ Canceled** - Отменен

### Просмотр логов:
```
Vercel Dashboard → Deployments → View Logs
```

### Уведомления:
```
Vercel Dashboard → Settings → Notifications
- Email о статусе деплоя
- Slack/Discord интеграция
```

---

## 🐛 Диагностика проблем с деплоем

### Проблема: Деплой не запускается

#### Шаг 1: Проверьте интеграцию
```
Vercel → Settings → Integrations → GitHub → Reconnect
```

#### Шаг 2: Проверьте webhook'и GitHub
```
GitHub → Repository → Settings → Webhooks
- Ищите Vercel webhook
- Status: Active (зеленый)
```

#### Шаг 3: Ручной триггер
```
Vercel → Trigger Deploy
```

### Проблема: Деплой падает с ошибкой

#### Шаг 1: Посмотрите логи
```
Vercel → Deployments → View Logs
```

#### Шаг 2: Проверьте переменные окружения
```
Vercel → Settings → Environment Variables
```

#### Шаг 3: Проверьте build скрипт
```json
{
  "scripts": {
    "build": "next build",
    "start": "next start"
  }
}
```

---

## ⚙️ Расширенные настройки

### 1. **Build Hooks** (для других CI/CD)
```
Vercel → Settings → Git → Deploy Hooks
- Создайте webhook URL
- Используйте для интеграции с Jenkins/GitLab CI
```

### 2. **Branch-specific деплой**
```javascript
// vercel.json
{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["fra1"],
  "functions": {
    "pages/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
```

### 3. **Игнорирование файлов**
```javascript
// vercel.json
{
  "functions": {
    "pages/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "buildCommand": "npm run build",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- package.json"
}
```

---

## 🚦 Статусы и их значения

| Статус | Цвет | Значение | Действие |
|--------|------|----------|----------|
| Building | 🟡 | Сборка в процессе | Ждать |
| Ready | 🟢 | Деплой успешен | Готово |
| Error | 🔴 | Ошибка сборки | Проверить логи |
| Canceled | ⚪ | Отменен пользователем | Перезапустить |

---

## 📞 Полезные команды

```bash
# Проверить статус последнего коммита
git log --oneline -1

# Создать тег для релиза
git tag v1.0.0
git push origin v1.0.0

# Force push (если нужно переписать историю)
git push origin main --force
```

---

## 🎯 Лучшие практики

### 1. **Всегда проверяйте перед push**
```bash
npm run build  # Локальная проверка
npm run lint   # Проверка кода
```

### 2. **Используйте feature ветки**
```bash
git checkout -b feature/new-feature
# Работаете здесь
git checkout main
git merge feature/new-feature
git push origin main
```

### 3. **Мониторьте логи**
- Включайте уведомления о деплоях
- Регулярно проверяйте статус

### 4. **Резервные планы**
- Всегда можно запустить деплой вручную
- Preview deployments для тестирования

---

## 🔗 Полезные ссылки

- [Vercel Deployments](https://vercel.com/docs/deployments/overview)
- [GitHub Webhooks](https://docs.github.com/webhooks/)
- [Vercel + GitHub Integration](https://vercel.com/docs/integrations/git)

---

## 💡 Pro tips

1. **Preview Deployments**: Каждый PR создает тестовый деплой
2. **Environment Variables**: Разные переменные для production/staging
3. **Build Caching**: Vercel кэширует зависимости для быстрой сборки
4. **Rollback**: Можно откатить к предыдущему деплою

**Автоматический деплой работает надежно - просто push'те в main!** 🚀