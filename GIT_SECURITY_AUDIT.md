# Аудит безопасности Git репозитория

## 🔍 Проверка наличия секретов в репозитории

### ✅ Что проверено:

1. **Файлы в .gitignore**
2. **История коммитов на наличие секретов**
3. **Текущие файлы в репозитории**
4. **Потенциальные утечки данных**

---

## 📊 Результаты проверки

### ✅ Хорошо защищено:

1. **`.gitignore` настроен правильно:**
   - `.env` файлы игнорируются
   - `.env.local`, `.env*.local` в ignore
   - `*.key`, `*.pem` файлы игнорируются
   - Логи и временные файлы игнорируются

2. **Нет реальных секретов в коде:**
   - В `lib/supabase.ts` используются только `process.env` переменные
   - В README.md только примеры (`your-project.supabase.co`, `your-anon-key`)
   - Нет хардкода реальных ключей

3. **Placeholder значения:**
   ```typescript
   supabaseUrl || 'https://placeholder.supabase.co'
   supabaseAnonKey || 'placeholder-key'
   ```

---

## ⚠️ Потенциальные проблемы:

### 1. NEXT_PUBLIC_ переменные доступны на клиенте

**Проблема:**
```typescript
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

Эти переменные **всегда** попадают в клиентский bundle Next.js, даже если они в `.env.local`.

**Риск:**
- Если кто-то получит доступ к репозиторию и соберет проект, он увидит эти ключи в bundle
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - это публичный ключ, но он все равно должен быть защищен RLS

**Решение:**
- ✅ RLS должен защищать данные (но сейчас RLS слабый)
- ⚠️ Не хранить чувствительные данные в `NEXT_PUBLIC_*` переменных

### 2. История коммитов

**Проверено:**
- Нет коммитов с реальными секретами
- В истории только примеры и placeholder значения

**Рекомендация:**
- Периодически проверять историю на наличие секретов
- Использовать инструменты типа `git-secrets` или `truffleHog`

### 3. Логирование в коде

**Проблема:**
```typescript
// lib/supabase.ts
console.log('  URL:', supabaseUrl ? `${supabaseUrl.substring(0, 30)}...` : '❌ NOT SET')
console.log('  Key:', supabaseAnonKey ? `${supabaseAnonKey.substring(0, 20)}...` : '❌ NOT SET')
```

**Риск:**
- В development режиме логируются части ключей
- Если логи попадут в git - может быть утечка

**Решение:**
- ✅ Логи только в development
- ⚠️ Убедиться, что логи не коммитятся

---

## 🔒 Рекомендации по безопасности

### Приоритет 1 (Критично):

1. **Проверить историю коммитов на секреты:**
   ```bash
   # Использовать git-secrets или truffleHog
   git log --all --source -p | grep -i "password\|secret\|key\|token"
   ```

2. **Убедиться, что .env.local не в git:**
   ```bash
   git ls-files | grep "\.env"
   # Должно быть пусто
   ```

3. **Проверить, нет ли реальных ключей в коде:**
   ```bash
   grep -r "https://.*\.supabase\.co" --exclude-dir=node_modules .
   # Должны быть только примеры
   ```

### Приоритет 2 (Важно):

1. **Использовать git-secrets для предотвращения коммитов с секретами:**
   ```bash
   git secrets --install
   git secrets --register-aws
   ```

2. **Настроить pre-commit hook:**
   - Проверять наличие секретов перед коммитом
   - Блокировать коммиты с потенциальными секретами

3. **Ротация ключей:**
   - Периодически менять ключи Supabase
   - Особенно если есть подозрения на утечку

### Приоритет 3 (Желательно):

1. **Использовать секреты из Vercel/других сервисов:**
   - Не хранить секреты локально
   - Использовать переменные окружения из CI/CD

2. **Мониторинг утечек:**
   - Использовать GitHub Secret Scanning
   - Настроить алерты на обнаружение секретов

---

## 📋 Чек-лист безопасности Git

- [x] `.env` файлы в `.gitignore`
- [x] `.env.local` в `.gitignore`
- [x] Нет реальных секретов в коде
- [x] Нет реальных ключей в истории коммитов
- [ ] Настроен git-secrets (рекомендуется)
- [ ] Настроен pre-commit hook (рекомендуется)
- [ ] Используется GitHub Secret Scanning (рекомендуется)

---

## 🎯 Итоговая оценка безопасности Git

**Оценка: 7/10**

**Что хорошо:**
- ✅ `.gitignore` настроен правильно
- ✅ Нет реальных секретов в коде
- ✅ Используются только переменные окружения

**Что можно улучшить:**
- ⚠️ Добавить git-secrets для предотвращения утечек
- ⚠️ Настроить pre-commit hooks
- ⚠️ Использовать Secret Scanning

**Риски:**
- 🟡 `NEXT_PUBLIC_*` переменные доступны на клиенте (но это нормально для публичных ключей)
- 🟢 Нет критических утечек секретов

---

## ⚠️ Важные замечания

1. **NEXT_PUBLIC_SUPABASE_ANON_KEY** - это публичный ключ, но:
   - Он должен быть защищен RLS политиками
   - Если RLS слабый - это проблема безопасности БД, а не Git

2. **SUPABASE_SERVICE_ROLE_KEY** - это секретный ключ:
   - ✅ Должен быть только в `.env.local` (не в git)
   - ✅ Не должен попадать в клиентский bundle
   - ⚠️ Проверить, что он не используется в `NEXT_PUBLIC_*`

3. **ADMIN_PASSWORD**:
   - ✅ Должен быть только в `.env.local`
   - ⚠️ Проверить, что нет fallback значений в production

---

## 🔍 Команды для проверки

```bash
# Проверить, нет ли .env файлов в git
git ls-files | grep "\.env"

# Проверить историю на наличие секретов
git log --all --source -p | grep -iE "password|secret|key|token|supabase.*\.co"

# Проверить текущие файлы на секреты
grep -r "https://.*\.supabase\.co" --exclude-dir=node_modules .

# Проверить, нет ли реальных ключей (JWT токены начинаются с eyJ)
grep -r "eyJ" --exclude-dir=node_modules .
```

---

## ✅ Вывод

**Текущее состояние: Безопасно** ✅

- Нет реальных секретов в репозитории
- `.gitignore` настроен правильно
- Используются только переменные окружения

**Рекомендации:**
- Добавить git-secrets для предотвращения будущих утечек
- Настроить мониторинг утечек секретов
- Периодически проверять историю коммитов
